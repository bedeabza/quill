# Repository instructions

## Branching

- Always work directly on `master` unless the user explicitly specifies another branch.
- Commit and push to `origin/master` by default. Do not create a feature branch unless requested.
